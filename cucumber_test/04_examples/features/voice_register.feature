# language: zh-CN
@formal @voice_register @module:voice-register
功能: 语音注册功能验证
  支持命令词和唤醒词学习、失败重试、删除、冲突、模板满和掉电保持的需求项目需要执行语音注册专项

  背景:
    假如 当前项目 profile 已加载
    并且 设备通过最小可测性门禁
    并且 已恢复干净基线

  @case_id:REG-CMD-001
  场景: 新学习的命令词应能触发原目标控制
    假如 已进入命令词学习模式
    当 按需求次数学习命令词别名
    并且 等待学习保存完成
    并且 重启设备
    并且 唤醒后播放新学习的命令词
    那么 协议口应捕获原目标控制协议

  @case_id:REG-FAIL-004 @negative
  场景: 学习失败耗尽后失败词不应残留生效
    假如 已进入唤醒词学习模式
    当 连续输入不符合规则的学习语料直到失败耗尽
    并且 重启设备隔离残留会话
    并且 播放失败学习的唤醒词
    那么 不应进入有效唤醒会话
    并且 后续业务命令不应产生控制协议

  @case_id:REG-CFG-005
  场景: 命令词模板满后再次学习应提示模板已满
    假如 已恢复干净基线
    当 在用例内主动填满命令词模板
    并且 等待所有学习保存完成
    并且 重启设备
    并且 再次进入命令词学习模式
    那么 日志应出现模板已满提示或对应 play id
