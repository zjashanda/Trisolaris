# language: zh-CN
@formal @numeric @module:volume-level @project:csk5062_xiaodu_fan
功能: 小度风扇音量默认值和档位验证
  小度风扇需求要求音量档位、默认音量和上下边界行为符合需求文档

  背景:
    假如 当前项目 profile 已加载
    并且 设备通过最小可测性门禁

  @case_id:CFG-VOL-001
  场景: 默认音量应符合需求值
    假如 已清除配置并烧录当前项目固件
    当 设备首次启动完成
    并且 使用音量边界探测法推断当前默认档位
    那么 默认音量档位应等于需求值

  @case_id:CFG-VOL-002
  场景: 总音量档位数应符合需求值
    假如 设备处于普通会话
    当 从当前音量单边探测到最小边界
    并且 从最小边界逐级增大到最大边界
    那么 可达音量档位总数应等于需求值

  @case_id:CFG-VOL-NEG-001 @negative
  场景: 最小音量继续调小不应越界
    假如 设备音量已调整到最小边界
    当 播放语音命令 "小声点"
    那么 运行音量不应低于最小边界
    并且 日志应出现需求定义的下溢播报或提示证据
