# csk3022_htt_clothes_airer 模块化验证池匹配结果

## 输入

- `项目需求/好太太晾衣机`

## 候选模块

| 模块 | 分数 | 命中关键词 | 处理建议 |
| --- | ---: | --- | --- |
| `active-passive-closed-loop` | 28 | 主动(6), 被动(32), MCU(18), 回包(10), 闭环(2), 本地状态(4), 状态刷新(2) | 读取模块并选择变体 |
| `requirement-ingestion` | 26 | 需求(17), 文档(10), 词表(1), 协议(37), 固件(4), Excel(8), 流程图(1) | 读取模块并选择变体 |
| `voice-switch-state-machine` | 19 | 语音功能关闭(3), 语音功能打开(6), 受限(6), 咚(1), 关闭语音(6) | 读取模块并选择变体 |
| `broadcast-play-id` | 14 | 播报(87), play id(1), 设置成功(2), 设置失败(8), 咚(1) | 读取模块并选择变体 |
| `persistence-config` | 14 | 掉电保存(5), 恢复出厂(4), 保存(5) | 读取模块并选择变体 |
| `volume-level` | 14 | 音量(22), 默认音量(3), 最大音量(4), 最小音量(2) | 读取模块并选择变体 |
| `passive-protocol` | 11 | 被动协议(1), 回包(10), 被动播报(27) | 读取模块并选择变体 |
| `wake-session` | 11 | 唤醒(14), 唤醒词(6), 超时(1) | 读取模块并选择变体 |
| `active-protocol` | 10 | 发送协议(14), 命令词(5) | 读取模块并选择变体 |
| `burn-and-gates` | 4 | 上电(4) | 读取模块并选择变体 |
| `voice-register` | 2 | 冲突(2) | 低分候选，按需求人工确认 |
| `fault-convergence` | 1 | 断言(1) | 低分候选，按需求人工确认 |
| `negative-abnormal` | 1 | 异常(1) | 低分候选，按需求人工确认 |

## 后续动作

1. 对高分模块读取对应 `references/validation-pool/*.md`。
2. 按当前需求选择变体，禁止直接套旧项目结论。
3. 生成当前项目方案、用例、断言和执行器映射。
4. 执行后将新增通用逻辑回灌验证池。
