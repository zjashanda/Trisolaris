# csk5062_xiaodu_fan 模块化验证池匹配结果

## 输入

- `/home/aitestu/.openclaw/skills/Trisolaris/项目需求/CSK5062小度风扇需求`

## 候选模块

| 模块 | 分数 | 命中关键词 | 处理建议 |
| --- | ---: | --- | --- |
| `requirement-ingestion` | 14 | 需求(41), 文档(1), 协议(1), tone(53), 固件(2) | 读取模块并选择变体 |
| `broadcast-play-id` | 10 | 播报(5), tone(53) | 读取模块并选择变体 |
| `fault-convergence` | 10 | FAIL(5), BLOCK(16) | 读取模块并选择变体 |
| `wake-session` | 10 | 唤醒(15), 唤醒词(13) | 读取模块并选择变体 |
| `volume-level` | 9 | 音量(13), 默认音量(1), 档位(1), 最大音量(1), 最小音量(1) | 读取模块并选择变体 |
| `voice-register` | 7 | 语音注册(2), 学习命令词(3), 学习唤醒词(1), 冲突(1) | 读取模块并选择变体 |
| `active-protocol` | 6 | 命令词(4), UART(2) | 读取模块并选择变体 |
| `persistence-config` | 6 | 掉电保存(3), 保存(3) | 读取模块并选择变体 |
| `active-passive-closed-loop` | 5 | voice(35) | 读取模块并选择变体 |
| `burn-and-gates` | 5 | ready(12) | 读取模块并选择变体 |
| `negative-abnormal` | 5 | BLOCK(16) | 读取模块并选择变体 |
| `voice-switch-state-machine` | 5 | voice(35) | 读取模块并选择变体 |
| `packaging-config-matrix` | 2 | package(2) | 低分候选，按需求人工确认 |

## 后续动作

1. 对高分模块读取对应 `references/validation-pool/*.md`。
2. 按当前需求选择变体，禁止直接套旧项目结论。
3. 生成当前项目方案、用例、断言和执行器映射。
4. 执行后将新增通用逻辑回灌验证池。
